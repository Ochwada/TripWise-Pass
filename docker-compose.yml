services:
 trippass:
    build: .
    container_name: trippass
    ports:
      - "9091:9091"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - tripwise-backend

networks:
  tripwise-backend:
    external: true
