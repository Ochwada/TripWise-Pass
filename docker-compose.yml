services:
 trippass:
    build: .
    container_name: trippass
    ports:
      - "9091:9080"   # host 9091 → container 9080
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - tripwise-backend

networks:
  tripwise-backend:
    external: true
